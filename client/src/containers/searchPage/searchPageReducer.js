
const searchPageReducer = (state = [], action) => {
    switch (action.type) {
        case "":
            return state;
        default:
            return state;
    }
}

export default searchPageReducer;